# Generated from templates/alacritty.toml.erb and configs/colors.yaml.

[font]
normal = { family = "Atkynson Mono Nerd Font", style = "Regular" }

[colors.primary]
background = "<%= theme_hex(:alacritty, :background) %>"
foreground = "<%= theme_hex(:alacritty, :foreground) %>"

[colors.cursor]
text = "<%= theme_hex(:alacritty, :background) %>"
cursor = "<%= theme_hex(:alacritty, :highlight) %>"

[colors.selection]
text = "<%= theme_hex(:alacritty, :background) %>"
background = "<%= theme_hex(:alacritty, :highlight) %>"

[colors.normal]
black = "<%= theme_hex(:alacritty, :black) %>"
red = "<%= theme_hex(:alacritty, :red) %>"
green = "<%= theme_hex(:alacritty, :cyan) %>"
yellow = "<%= theme_hex(:alacritty, :yellow) %>"
blue = "<%= theme_hex(:alacritty, :blue) %>"
magenta = "<%= theme_hex(:alacritty, :magenta) %>"
cyan = "<%= theme_hex(:alacritty, :cyan) %>"
white = "<%= theme_hex(:alacritty, :white) %>"

[colors.bright]
black = "<%= theme_hex(:alacritty, :light_gray) %>"
red = "<%= theme_hex(:alacritty, :red) %>"
green = "<%= theme_hex(:alacritty, :cyan) %>"
yellow = "<%= theme_hex(:alacritty, :warning) %>"
blue = "<%= theme_hex(:alacritty, :blue) %>"
magenta = "<%= theme_hex(:alacritty, :magenta) %>"
cyan = "<%= theme_hex(:alacritty, :cyan) %>"
white = "<%= theme_hex(:alacritty, :background) %>"
